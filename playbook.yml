---
- name: Installer ClamAV
  hosts: all
  become: yes
  become_method: sudo
  become_user: root
  become_flags: '-H'  

  environment:
    ANSIBLE_REMOTE_TMP: /tmp/.ansible
    ANSIBLE_HOST_KEY_CHECKING: "False"
  
  roles:
    - .
