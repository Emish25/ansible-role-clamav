---
- name: Installer ClamAV
  hosts: all
  become: yes
  become_method: sudo
  become_user: root
  vars:
    ansible_become_password: "X"  # ← ICI aussi !
  
  roles:
    - role: .
      become: yes  # ← Forcer become sur le rôle
