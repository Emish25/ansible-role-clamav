---
- name: Installer ClamAV
  hosts: all
  become: yes
  become_method: sudo
  become_user: root
  vars:
    ansible_become_password: "Ayden@0209"  # ← ICI aussi !
  
  roles:
    - role: .
      become: yes  # ← Forcer become sur le rôle
